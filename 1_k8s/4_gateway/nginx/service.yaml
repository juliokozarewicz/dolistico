apiVersion: v1
kind: Service
metadata:
    name: nginx-gateway
    namespace: production
spec:
    type: ClusterIP
    selector:
        app: nginx-gateway
    ports:
        - port: 80
          targetPort: 80